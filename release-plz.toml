# release-plz tracks each publishable crate by its own Cargo.toml version.
# Helper packages with publish = false are skipped automatically.
[workspace]
# Dependency updates follow the changed manifests for each released crate.
dependencies_update = true
git_release_enable = true
# Publishable crates are released in dependency order.
