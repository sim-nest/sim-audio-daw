# Live audio graph config (descriptor)

Documents a live audio graph configuration (queues, steady-state). Real-time audio scheduling runs on
a device outside the cookbook sandbox eval stack, so the config is documented rather than run.
