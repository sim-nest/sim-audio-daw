id = "modeled-coreaudio"
title = "Enumerate modeled CoreAudio devices"
purpose = "Verify deterministic CoreAudio device discovery without physical hardware."
command = "cargo test -p sim-lib-stream-coreaudio fake_backend_enumerates_default_input_and_output"
order = 10
tags = ["stream", "coreaudio", "modeled", "inventory", "test-command"]
requires = ["stream-coreaudio", "codec/lisp"]
