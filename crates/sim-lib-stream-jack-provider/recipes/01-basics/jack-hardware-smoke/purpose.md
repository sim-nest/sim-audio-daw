# JACK hardware smoke (descriptor)

Documents a JACK audio-hardware smoke test, guarded behind an env flag. It requires a real JACK server
and device, infeasible in the cookbook sandbox, so it is documented rather than run.
