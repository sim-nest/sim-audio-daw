# JACK provider load (descriptor)

Documents loading the JACK provider under an audio capability. Acquiring the capability and binding the
provider are host concerns outside the cookbook sandbox eval stack.
