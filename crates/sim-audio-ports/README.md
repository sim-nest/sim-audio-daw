# sim-audio-ports

Domain-owned contracts for native audio-device and plugin realization. Platform
capsules implement these ports; graph, DSP, stream, and plugin protocol crates
consume them without target predicates or native API knowledge.
