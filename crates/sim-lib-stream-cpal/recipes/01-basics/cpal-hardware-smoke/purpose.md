# CPAL hardware smoke (descriptor)

Documents a CPAL audio-hardware smoke test, guarded behind an env flag. It requires a real audio
device, which is infeasible in the cookbook sandbox, so it is documented rather than run.
