# VST3 gain plugin (descriptor)

Documents the VST3 gain plugin descriptor (buses and parameters). A plugin ABI descriptor: the plugin is
instantiated and run inside an audio host, not the cookbook sandbox eval stack, so it is
documented rather than run.
