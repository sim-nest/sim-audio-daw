# Bounded Wasm gain plugin

Runs the deterministic Wasm loader test that grants `plugin.audio.wasm`,
loads the fixture gain plugin, and proves the descriptor is available through
the shared plugin instance surface.
