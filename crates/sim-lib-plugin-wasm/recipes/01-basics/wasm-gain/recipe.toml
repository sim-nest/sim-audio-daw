id = "wasm-gain"
title = "Load a bounded Wasm gain plugin"
command = "cargo test -p sim-lib-plugin-wasm wasm_plugin_load_allowed_with_capability --features wasm-plugin"
order = 10
tags = ["plugin", "wasm", "capability", "bounded", "test-command"]
requires = ["plugin-wasm", "wasm-plugin", "codec/lisp"]
