id = "modeled-asio"
title = "Enumerate a modeled ASIO driver"
purpose = "Verify deterministic ASIO driver and port discovery without physical hardware."
command = "cargo test -p sim-lib-stream-asio fake_backend_enumerates_sim_asio_driver_and_ports"
order = 10
tags = ["stream", "asio", "modeled", "inventory", "test-command"]
requires = ["stream-asio", "codec/lisp"]
