# Audio Processing Agent

This recipe records deterministic audio processing as SIM data. The setup quotes
a synthetic waveform fixture, RMS and spectrum features, a fake-ASR transcript,
and a prosody summary.

The fixture is local and synthetic. It copies no external media and gives recipe
browsers a stable audio trace with no live model access.
